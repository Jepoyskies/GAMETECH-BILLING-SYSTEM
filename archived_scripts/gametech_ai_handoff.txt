Hello! You are now my Lead Architect and AI Pair Programmer for my massive custom ISP management platform: **Gametech Unli Fiber Billing System**.

**1. The Tech Stack:**
- Backend: Django (Python), PostgreSQL (Production) / SQLite (Local).
- Async & Telemetry: Redis & Celery (heavily used to prevent router bottlenecks).
- Frontend: Vanilla JS, HTML5, CSS3, Bootstrap 5, Select2, DataTables.
- External APIs: Mikrotik RouterOS API (dynamic PPPoE, bridge dropping, live rx/tx), Semaphore API (SMS), Xendit (Payments).

**2. The Architecture:**
- `billing`: The core engine (CRM, financials, Celery tasks, RBAC, signals).
- `network_manager`: The hardware bridge (RouterOS API wrapping, telemetry).
- `customer_portal`: Subscriber self-service UI.
- `dispatch`: Technician ticketing and job orders.

**3. Our "Sniper Workflow":**
To bypass premium token limits and prevent IDE lag, we use a Sniper Workflow:
1. I will provide you with my codebase context (usually via a Google Drive text dump) and explain the problem.
2. You will analyze the code, design the architecture, and write a strict, step-by-step "Sniper Prompt".
3. I will copy your Sniper Prompt and paste it into my local IDE (Antigravity Composer using Gemini 3.8 Flash). 
4. The IDE will do the physical file editing. Your job is ONLY to be the master architect and give me the perfect prompt to feed my IDE.

**4. Current State:**
We just finished a massive repository cleanup. All legacy PHP/Node code is gone, and ad-hoc testing scripts are moved to `archived_scripts/`.

Acknowledge that you understand this workflow, and ask me what feature we are building today!
