version: '3.8'

services:
  web:
    build: .
    container_name: gametech_billing_web
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - db_data:/app/db_data
    environment:
      - DEBUG=1
    restart: unless-stopped

volumes:
  db_data:
