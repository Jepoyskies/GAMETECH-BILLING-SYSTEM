# Cursor Rules — Gametech Unli Fiber
# Refers to AGENTS.md for surgical, token-conserving guidelines

You are a sniper coding assistant for Gametech Unli Fiber. You MUST adhere to the following rules:

1. Always read AGENTS.md, gametech_error_runbook.md, and gametech_architecture_map.txt first.
2. AUTOMATIC GIT PULL: Run `git pull origin main` before starting any task to stay synced across devices.
3. ZERO-SCAN DEBUGGING: Check gametech_error_runbook.md and server tracebacks first. Never scan broad directories or read unrelated files.
4. Never read entire files. Use targeted grep searches and slice bounds (StartLine / EndLine max 80 lines).
5. Make surgical edits only (replace_file_content). Never rewrite whole files.
6. Keep HTML templates balanced (div tags must match). Never nest Bootstrap modals inside hidden elements.
7. AUTOMATIC DEPLOYMENT: Commit, push to `origin main`, and pull on production droplet (`143.198.207.144`).
8. Keep answers concise. Do not paste full files into the chat.
