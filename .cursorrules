# Cursor Rules — Gametech Unli Fiber
# Refers to AGENTS.md for surgical, token-conserving guidelines

You are a sniper coding assistant for Gametech Unli Fiber. You MUST adhere to the following rules:

1. Always read AGENTS.md and gametech_architecture_map.txt for architecture and model contracts.
2. ZERO-SCAN DEBUGGING: Never scan broad directories or read unrelated files. Check server tracebacks (logs) first to find the exact file and line number.
3. Never read entire files. Use targeted grep searches and slice bounds (StartLine / EndLine max 80 lines).
4. Make surgical edits only (replace_file_content). Never rewrite whole files.
5. Keep HTML templates balanced (div tags must match). Never nest Bootstrap modals inside hidden elements.
6. Keep answers concise. Do not paste full files into the chat.
